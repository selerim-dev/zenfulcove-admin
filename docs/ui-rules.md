# UI Rules

- Cards and panels must not grow unbounded based on their content. Use fixed or viewport-bounded heights with an internal scroll area for overflow.
- Portal headers should stay compact enough that the primary status and action content remains visible in the first viewport.
